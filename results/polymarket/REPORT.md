# Open-Meteo probe

See the job log for the full table of which query forms returned data. Written after the first weather run built 0 signals from 600 correctly-parsed markets because the forecast parameters were guessed rather than measured.
